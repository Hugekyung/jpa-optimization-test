# 작업 규칙

## 작업 범위

- 작업자가 지시한 작업만 수행한다.
- 작업자가 요청하지 않은 추가 작업, 개선, 리팩터링, 문서 수정은 임의로 진행하지 않는다.
- 추가 작업이 필요해 보이더라도 먼저 작업자에게 확인한다.

## 문서 충돌

- 작업 진행 중 PRD, TASK 문서 또는 작업자 지시가 서로 충돌하는 경우 임의로 선택하지 않는다.
- 충돌 내용을 정리한 뒤 작업을 멈추고 작업자에게 재질문한다.
- 작업자에게 확인받기 전까지 충돌 지점과 직접 관련된 변경을 진행하지 않는다.

## 작업 결과 보고

- 작업이 끝나면 수행한 작업, 주요 변경 내용, 검증 결과를 핵심 내용 위주로 간략하게 요약한다.
- 필요하면 표, 텍스트 다이어그램 등 시각화 자료를 적극적으로 사용한다.
- 결과 보고에는 완료한 내용과 확인하지 못한 내용 또는 남은 문제를 구분해 작성한다.

## 서브 에이전트 사용

- 단계별 작업 중 토큰을 많이 소모하는 불필요한 서브 에이전트 기반 작업은 지양한다.
- 서브 에이전트는 작업자가 명시적으로 요청했거나, 작업을 실질적으로 단축하는 독립적인 작업이 있을 때만 사용한다.
- 단순한 탐색, 수정, 검증은 가능한 한 현재 작업자가 직접 수행한다.

## 코드 변경 작업

- 각 단계를 수행하면서 코드 또는 테스트 코드를 수정해야 하는 경우, 별도의 Git worktree와 branch를 생성한 뒤 작업한다.
- 작업 대상 branch와 worktree를 먼저 확인하고, 기존 작업자의 변경사항과 섞이지 않도록 관리한다.
- 단순 문서 확인이나 작업 결과 보고처럼 코드 변경이 없는 작업에는 별도 worktree와 branch를 만들지 않는다.

## 성능 비교 검증

- 성능 확인을 위한 각 단계가 끝날 때마다 지금까지 구현한 모든 조회 방식을 함께 비교한다.
- 모든 방식은 동일한 테스트 데이터, 조회 조건, 정렬 조건, 페이지 조건, 응답 범위를 사용한다.
- 조회 조건과 조회 결과가 동일한지 먼저 확인한 뒤 SQL 실행 횟수, 실행 시간, 조회 컬럼과 데이터 양을 비교한다.
- 새 단계의 결과만 확인하지 말고 이전 단계의 결과와 함께 출력한다.
- 비교 결과에는 최소한 다음 내용을 포함한다.
  - 조회 방식
  - 조회 조건
  - 조회 결과 건수와 주요 데이터 일치 여부
  - SQL 실행 횟수
  - 실행 시간
  - 조회된 Entity 또는 컬럼 범위
  - 방식별 차이와 주의점
- 비교 결과는 표나 텍스트 다이어그램 등으로 간략하게 정리해 작업 결과에 포함한다.
